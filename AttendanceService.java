class AttendanceService {

    public boolean validateScan(Student student, String qrData) {
        // check if already scanned
        // check if QR is valid
        // check time validity
        return true;
    }

    public void recordAttendance(Student student, ClassSession session) {
        // only record if valid
    }
}